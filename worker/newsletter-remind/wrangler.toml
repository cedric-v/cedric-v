name = "cedric-v-newsletter-remind"
main = "index.js"
compatibility_date = "2025-01-24"

# Quotidien à 6 h 45 UTC. L'endpoint /api/newsletter-remind ignore les
# inscriptions de moins de 20 h et purge la file au-delà de 7 jours,
# donc un décalage d'exécution du cron est sans conséquence.
[triggers]
crons = ["45 6 * * *"]

[vars]
# Alertes d'échec du cron (le Worker envoie directement via Mailjet).
# Secrets requis en plus : MAILJET_API_KEY, MAILJET_API_SECRET.
ALERT_FROM_EMAIL = "cedric@e.cedricv.com"
ALERT_TO_EMAIL = "cedric@cedricv.com"

[observability]
enabled = true
